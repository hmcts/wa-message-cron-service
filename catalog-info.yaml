apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "wa-message-cron-service"
  annotations:
    # This must match folder-name/job-name in Jenkins.
    jenkins.io/job-full-name: cft:HMCTS_j_to_z/wa-message-cron-service
    github.com/project-slug: hmcts/wa-message-cron-service
  tags:
    - nodejs
  links:
    - url: https://hmcts-reform.slack.com/app_redirect?channel=wa-tech
      title: "#wa-tech on Slack"
      icon: chat
spec:
  type: service
  lifecycle: production
  owner: dcd_group_wa_v2
